import styles from './Input.module.scss';

const Input = (props) => {
    const{} = props;


    return <button className={classN} disabled={disabled} onClick={onClick}>{children}</button>
};

export default Input;